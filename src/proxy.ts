import { clerkMiddleware } from "@clerk/nextjs/server";

// Resource authorization lives in the protected app layout. Proxy establishes
// Clerk request state for Server Components without relying on path matching.
export default clerkMiddleware();

export const config = {
  matcher: [
    "/((?!_next|[^?]*\\.(?:html?|css|js(?!on)|jpe?g|webp|png|gif|svg|ttf|woff2?|ico|csv|docx?|xlsx?|zip|webmanifest)).*)",
    "/(api|trpc)(.*)",
    "/__clerk/(.*)",
  ],
};
